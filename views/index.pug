doctype html
html
  head
    title Meister
    meta(charset="UTF-8")
    meta(name="viewport", content="width=device-width, initial-scale=1, user-scalable=no")
    meta(name="apple-mobile-web-app-capable", content="yes")
    meta(name="mobile-web-app-capable", content="yes")
    meta(http-equiv="X-UA-Compatible", content="IE=edge,chrome=1")

    link(rel="icon", href="/img/favicon.ico")
    link(href="https://fonts.googleapis.com/css?family=Open+Sans:400,300,300italic,400italic,700,700italic", rel="stylesheet", type="text/css")
    if mode === 'production'
      link(href=furl("/app.css"), type="text/css", rel="stylesheet")
  body
    if !login
      #meister-container
      script(src=furl("/common.js"))
      script(src=furl("/bundle.js"))
    else
      header
        .boundary
          .logo
            img(src="/img/freshy-logo.png")
      section#bigimage_1
        .boundary
          .row
            .col-sm-6
              #login(style="width: 125px; margin: 44px auto 0; border: 1px solid #aaa; border-radius:4px; padding: 10px;")
                a(href="/auth/github") Enter if you dare...
                  img(src="/img/dragon.jpg", width="125") 
      footer
      script(src=furl("/common.js"))
      script(src=furl("/bundle_login.js"))
